<h1>Прохождение <%= @test_passage.test.title %> теста</h1>

<b>
  <%= @test_passage.current_question.body %>
</b>

<p>
  <b>
    Вопрос: <%= @test_passage.current_question_position %> из <%= @test_passage.total_test_question%>
  </b>
</p>

<%= form_with url: test_passage_path(@test_passage), local: true, method: :put do |form| %>
  <%= form.collection_check_boxes :answer_ids, @test_passage.current_question.answers, :id, :body,
   include_hidden: false do |b| %>
    <p>
      <%= b.check_box id: "answer_ids_#{b.object.id}" %>
      <%= b.label %>
    </p>
  <% end %>

  <p> <%= form.submit 'Дальше' %></p>
<% end %>
