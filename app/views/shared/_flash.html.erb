<div class="row justify-content-center"> 

  <% flash.each do |key, message| %>
    <div class="alert <%= flash_message(key)%>">
      <%= message.html_safe %>
    </div>
  <% end %>
</div>

 <%#= content_tag :p, message, class: "flash #{key}" %> 
