<tr>
  <td><%= test.title %></td>
  <td><%= test_level(test) %></td>
  <td><%= test.questions.count %></td>
  <td><%= button_to t('buttons.start_test'), start_test_path(test), class: "btn btn-outline-info" %></td>
</tr>
